import 'package:flutter/material.dart';

class AIAnalyzeScreen extends StatelessWidget {
  const AIAnalyzeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AI Analyze')),
      body: const Center(child: Text('AI Analyze Screen (Empty)')),
    );
  }
}
